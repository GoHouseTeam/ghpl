# ghpl
 ghpl is a programming language written in Python

# How to run the compiler
 To run the compiler you have to write in a terminal:
 * In Linux:
    `python3 evolve.py` or `python3.6 evolve.py` or `python3.8 evolve.py`
 * In Windows:
    `py3 evolve.py`
